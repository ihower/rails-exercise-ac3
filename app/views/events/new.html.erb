<%= form_for @event, :url => events_path, :html => { :multipart => true, :role => "form" } do |f| %>

  <%= render "form",  :f => f %>
  <%#= render :partial => "form", :locals => { :f => f } %>

  <%= f.submit "Create", :class => "btn btn-primary" %>

<% end %>